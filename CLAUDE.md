# 우리아이 꿈 자람 터 — 개발 지침 (v4.0)

## 목적/배포
- 내 아이 학습관리 + 비개발자 지인 공유.
- Next.js 웹앱을 Vercel에 단일 배포(전원 자동 최신).
- 지인은 설치 없이 링크 접속·가입·본인 API 키 입력으로 사용.

## 스택
- Next.js + TypeScript, Supabase(Postgres+인증+저장소+Edge Functions, RLS 필수)
- AI 호출은 Edge Function에서 '사용자별 암호화 키(BYOK)'로만 수행.

## 원칙
1. 개인 데이터 테이블은 account_id 기준 RLS로 격리. 문항만 공용.
2. 사용자 API 키는 암호화 저장, 앱·타 사용자 노출 금지.
3. 아이 응답은 학년 눈높이 + 안전 가드레일.
4. 학년은 데이터 필드일 뿐, 엔진은 학년 불변(성장형 확장).

## 현재 단계
P0(기반) → P1(오늘의 미션 루프). 첫 콘텐츠는 초등 3학년 수학(seed_math_grade3.sql).

## 하지 말 것
- 결제/구독/인앱결제(개인·소규모 공유, 불필요)
- 생기부(세특) 문장 대필(탐구활동 제안만)
- 사용자 키를 클라이언트로 내려보내기
